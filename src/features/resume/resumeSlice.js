import { createSlice } from '@reduxjs/toolkit';

const resumeState = {
    isLoading: false,   
}

const resumeSlice = createSlice({
    name: 'resume',
    initialState: resumeState,
    reducers: {
        
    }
});