import loader from '../assets/gifs/loader.svg';

const Loading = () => {
    return (
        <div className="loading">
            <img src={loader} />
        </div>
    );
}

export default Loading;